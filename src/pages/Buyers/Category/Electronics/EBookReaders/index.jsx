import Card from '../card';
import imagesList from './images';

const EBookReaders = () => {
    return (
        <>
            <Card imagesList={imagesList} subCategory={'EBookReaders'} />
        </>
    );
}

export default EBookReaders;