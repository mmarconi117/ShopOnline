import Card from '../card';
import imagesList from './images';

const GamingAccessories = () => {
    return (
        <>
            <Card imagesList={imagesList} subCategory={'GamingAccessories'} />
        </>
    );
}

export default GamingAccessories;