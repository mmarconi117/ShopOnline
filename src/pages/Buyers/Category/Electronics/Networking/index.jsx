import Card from '../card';
import imagesList from './images';

const Networking = () => {
    return (
        <>
            <Card imagesList={imagesList} subCategory={'Networking'} />
        </>
    );
}

export default Networking;