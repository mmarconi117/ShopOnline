import Card from '../card';
import imagesList from './images';

const Hardware = () => {
    return (
        <>
            <Card imagesList={imagesList} subCategory={'Hardware'} />
        </>
    );
}

export default Hardware;